// ... existing code ...

def ask_question(question, default_response="Sorry, I didn't understand that."):
    print(question)
    user_input = input().strip()

    if not user_input:
        return default_response

    # existing logic for processing the input
    # ... existing code ...

// ... existing code ...